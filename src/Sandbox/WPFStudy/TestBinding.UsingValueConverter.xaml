<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Simple Window" 
    x:Class="WPFStudy.TestBinding.UsingConversion" 
    xmlns:local="clr-namespace:WPFStudy.TestBinding" >
    
    <Window.Resources>
        <local:Photos x:Key="photos"/>
        <local:CountToBackgroundConverter x:Key="myConverter"/>
        <BooleanToVisibilityConverter x:Key="booltoVis"/>
    </Window.Resources>

    <StackPanel>
        <Label 
            Background="{Binding Path=Count, Converter={StaticResource myConverter}, Source={StaticResource photos}}" 
        />
        <Button
            Content="Add"
            Click="OnAdd"
        />
        <CheckBox x:Name="checkBox">Show Status Bar</CheckBox>
        <StatusBar 
            Visibility="{Binding ElementName=checkBox, Path=IsChecked, Converter={StaticResource booltoVis}}">
            …
        </StatusBar>
    </StackPanel>
</Window>
